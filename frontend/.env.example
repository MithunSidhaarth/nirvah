# Point this at your deployed backend once it is live.
# Locally it already matches the backend's default port, so
# `npm run dev` in /backend just works without any setup.
VITE_API_BASE_URL=http://localhost:4000/api
